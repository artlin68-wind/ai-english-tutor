# AI 英語老師 App — 給 Code（AI 編碼工具）的 Prompt 規劃書

這份文件是要你**複製貼給 AI 編碼工具**（Claude Code、Cursor、Copilot 等）來實際生出 App 的指令規劃。
用法：先貼「Part A 起手式 Prompt」建立專案骨架，再依「Part C 分階段指令」一段一段餵，一次做一個模組，做完測試再做下一個。

---

## Part 0 · 為什麼這樣規劃

你的需求是「能裝在 iPhone、背景生動、讓人一直想用、真人老師形象、多情境對話、糾正發音、提醒窮詞、測驗例句」。
要能裝在 iPhone 且跨平台，建議用 **Flutter**（一套程式碼同時打包 iOS + Android，UI 動畫能力強、適合做生動介面）。
真 AI 對話與發音評測需要後端代理金鑰（不能把 API 金鑰放進 App），所以架構含一個輕量後端。

---

## Part 0.5 · 硬性限制（語音必須零金鑰、零 MP3）

> 這是不可違反的架構原則，請 code 全程遵守。

1. **禁止使用需付費或需申請 API Key 的雲端語音服務**（如 Google Cloud TTS、OpenAI TTS、Amazon Polly）。
2. **禁止在專案包內塞入大量單字 MP3 音檔**，避免 App 過大。
3. **發音一律用「即時文字轉語音」而非「播放預錄音檔」**：使用手機作業系統內建的離線 TTS 引擎——iOS 的 AVSpeechSynthesizer、Android 的 TextToSpeech，在 Flutter 以 `flutter_tts` 套件呼叫。免費、離線、系統自帶，10000 單字不需任何音檔。
4. **辨識學生發音**也只用裝置內建離線引擎——iOS Speech / Android SpeechRecognizer，Flutter 以 `speech_to_text` 呼叫。免費、不需金鑰。
5. **發音評分**不得依賴付費雲端服務：用 `speech_to_text` 回傳的辨識文字與信心值，比對目標句做詞級/音節級評分即可。（音素級精準評分屬選配，若日後要用付費服務須另外開關，預設關閉。）

TTS 音質提醒：iOS/Android 內建語音品質已足夠；iPhone 使用者可在「設定 → 輔助使用 → 朗讀內容 → 語音」免費下載高品質英語語音（Enhanced/Premium），App 會自動採用，無需你付費或改程式。

---

## Part A · 起手式 Prompt（第一段就貼這個）

> 複製以下整段，貼給你的 AI 編碼工具。

```
你是一位資深 Flutter 工程師。請幫我從零建立一個 iOS/Android App 專案，主題是「給台灣高中生的 AI 英語口說老師」。

【產品目標】
- 使用者是台灣高中生，英文程度約 CEFR B1、單字量約 10000 字（以學測 7000 字為核心）。
- 核心體驗：選一位老師形象 → 選情境 → 跟 AI 老師用英文面對面對話 → 即時糾正發音與用詞 → 卡住時提醒可用句子 → 穿插測驗與例句。
- 介面要「生動、讓人每天想打開」：動態漸層背景、角色動畫、連續打卡、經驗值與等級、獎勵動效。

【技術棧】
- 前端：Flutter（最新穩定版），狀態管理用 Riverpod，路由用 go_router。
- 本地資料：sqflite 或 isar 儲存單字庫、學習進度、每日打卡。
- 語音：flutter_tts（老師發音）、speech_to_text（辨識學生發音）——兩者皆使用手機作業系統內建的離線引擎，禁止任何需付費/需 API Key 的雲端語音服務，且不得在專案內塞單字 MP3（發音一律即時文字合成）。
- 後端：一個輕量 API 代理（Node.js + Express 或 Python FastAPI），負責保管 LLM 金鑰、轉發對話請求、做發音評分回饋。App 不得直接存放任何第三方金鑰。
- 目標：可在 iPhone 上安裝執行（先用開發者模式/TestFlight，之後上 App Store）。

【這一步只做這些】
1. 建立 Flutter 專案結構（features/ 分層：avatar、scenario、chat、pronunciation、quiz、progress、shared）。
2. 設定主題（深色、圓角、Noto Sans TC 字型），做出一個會緩慢流動的多彩漸層動態背景元件 AnimatedBackground，全 App 共用。
3. 做底部導覽（對話 / 測驗 / 我的進度）與空白頁面骨架。
4. 先不接真 API，畫面用假資料。

請先輸出：專案資料夾結構、pubspec.yaml 依賴、AnimatedBackground 元件、主題設定、以及可執行的 main.dart。做完告訴我如何在模擬器執行。
```

---

## Part B · 完整功能規格（給 code 當需求依據，可整段附在起手式後面）

### B1. 老師形象（Avatar）
提供至少兩位可選老師：**帥哥 Ryan**、**美女 Emma**，後續可擴充。
- 第一版用高品質插畫／2D 立繪 + 簡單動畫（眨眼、說話嘴型、點頭）。
- 進階版可接 Live2D 或 3D（Rive / Live2D Cubism SDK）做「真實呈現」的動態表情。
- 老師性別對應不同 TTS 語音（男聲／女聲）。

### B1.5 視覺方向：角色 + 情境實景背景（沉浸式，對標 TOKO AI）
對話頁採「角色站在對應情境實景中」的沉浸式排版（如參考圖：3D 少女站在咖啡廳前，半透明對話框浮在上方）。這是本 App「生動、想一直用」的核心，務必落實。

**圖層結構（由後到前）：**
```
Layer 1 場景背景  依情境切換的實景圖（含景深模糊、光影），可加緩慢視差/微縮放動畫
Layer 2 角色立繪  老師 3D/Live2D/2D 立繪，會眨眼、說話嘴型、點頭、待機小動作
Layer 3 對話 UI   半透明毛玻璃對話框（英文 + 中文小字 + 播放鈕）浮在下方
Layer 4 控制列    麥克風大圓鈕、語音波形、翻譯/慢速/重播小圖示（參考圖底部樣式）
```

**每情境對應實景背景：**
| 情境 | 場景背景 |
|---|---|
| 生活應用 | 校園、房間、街角咖啡廳 |
| 旅遊 | 機場報到櫃檯、飯店大廳、觀光街景 |
| 購物 | 服飾店、超市貨架 |
| 書信往來 | 書桌、圖書館（偏靜態，可放筆記本特寫） |
| 點餐 | 咖啡廳露台、餐廳座位（同參考圖） |

**角色動畫需求：** 待機（呼吸/眨眼）、說話（嘴型 + 手勢）、鼓勵（點頭/微笑/比讚）、思考（歪頭）。用 Rive 或 Live2D 綁定，依對話狀態切換。

**選單/載入頁**：維持流動的多彩漸層動態背景（活潑、輕量）；**對話頁**才切換為情境實景 + 角色。兩者分工：漸層負責活潑導覽，實景負責沉浸對話。

### B2. 情境系統（可擴充）
初始情境：生活應用、旅遊、購物、書信往來、點餐。資料驅動設計——每個情境是一筆 JSON（名稱、圖示、簡介、開場白、目標句型、關鍵字彙），新增情境只要加資料，不用改程式。之後可加：面試、看醫生、電話英語、校園生活等。

### B3. 對話引擎
- **真 AI 模式**：呼叫後端 → LLM（system prompt 設定：友善的高中英語老師、B1 程度、句子簡短、每次結尾提問、發現錯誤用「✏️」溫和糾正、學生卡住用「💡」給句子、英文後附中文小翻譯）。
- 保留完整對話歷史做上下文。
- 語音輸入輸出：學生可用麥克風說，App 用 speech_to_text（裝置內建、離線、免費）轉文字送出；老師回覆用 flutter_tts（裝置內建、離線、免費、免 MP3）即時合成唸出。全程不使用任何付費雲端語音 API。

### B4. 發音糾正（全程離線、零金鑰）
- 學生說出目標句 → speech_to_text（裝置內建）取得辨識文字與信心值 → **本地**比對目標句，算出吻合度分數（詞比對 + 音節比對）。
- 回饋：吻合度百分比 + 分級評語（清楚／再放慢／逐音節重試）+ 一鍵「聽正確發音」（flutter_tts 即時合成，不放 MP3）。
- 「聽正確發音」可調慢速（rate 調低）讓學生跟讀。
- 選配（預設關閉、非必要不做）：若日後想要音素級精準評分，可接付費語音評測服務——但這違反免費原則，須另設開關由你決定，預設一律走上述本地免費方案。

### B5. 窮詞提醒（卡住時幫你）
- 偵測學生「很久沒回應」或「回答過短」→ 主動彈出可用句子與關鍵字。
- 提供「💡 想不出來」按鈕：顯示 2–3 句情境常用句 + 中文對照。
- 真 AI 模式下，直接讓 LLM 針對當前語境給提示。

### B6. 測驗與例句
- 單字測驗：選擇題（字義／填空），作答後顯示解析與例句。
- 例句朗讀、加入「我的生字本」。
- 題庫依學測分級（必備／進階／延伸），支援間隔複習（SRS）。

### B7. 生動介面與留存（讓人一直想用）
- 全 App 動態漸層背景 + 角色動畫 + 進場微動效。
- 遊戲化：每日打卡連續天數、經驗值 XP、等級、達成徽章、每日任務。
- 每日進度：今日單字量、對話次數、發音平均分，視覺化圓環。
- 推播提醒（flutter_local_notifications）每天邀請回來練習。

### B8. 資料結構（建議）
```
Word { id, en, kk音標, 中文, 詞性, 例句[], 難度分級, 情境tag[], 圖片 }
Scenario { id, 名稱, 圖示, 簡介, 開場白, 目標句型[], 關鍵字彙[], 背景圖, 角色動作預設 }
Progress { 日期, 打卡連續天數, XP, 等級, 今日單字數, 對話數, 發音平均分 }
ChatMessage { 角色, 內容英文, 內容中文, 發音分數 }
```

---

## Part C · 分階段開發指令（一段一段餵給 code）

> 建議照順序，每段做完在模擬器測過再進下一段。

**指令 1 — 動態背景與主題**（見 Part A 起手式，已含）

**指令 2 — 老師選擇頁**
```
做「選擇老師」頁面：兩張卡片（Ryan 帥哥 / Emma 美女），含立繪與簡介，選取有高亮動畫。選定後存進 Riverpod 狀態，並用對應性別設定 flutter_tts 的語音。
```

**指令 3 — 情境選單（資料驅動）**
```
建立 scenarios.json（生活/旅遊/購物/書信/點餐，每筆含名稱、圖示、簡介、開場白、目標句型、關鍵字彙）。做情境選單頁讀取 JSON 動態產生清單，並預留「＋更多情境」入口。點擊進入對話頁。
```

**指令 4 — 沉浸式對話頁 UI + 本地腳本模式**
```
做沉浸式聊天介面（對標 TOKO AI，見參考圖），四層結構：
(1) 最底層是情境實景背景圖（依 scenario.背景圖 切換，加緩慢視差/微縮放）；
(2) 中層是老師角色立繪（Rive/Live2D，先放靜態 PNG 佔位也可），有待機與說話動畫；
(3) 上層是半透明毛玻璃對話框，顯示老師英文 + 中文小字 + 🔊播放鈕，學生訊息也浮層顯示；
(4) 底部控制列：大麥克風圓鈕 + 語音波形 + 慢速/重播/翻譯小圖示。
先用情境 JSON 的腳本做引導式對話與簡單比對評分，不接真 API。加「💡想不出來 / 🔁再說一次 / 🇹🇼中文」三顆快捷鈕。角色會依對話狀態切換待機/說話/鼓勵動作。
```

**指令 5 — 語音輸入輸出**
```
整合 speech_to_text（英文辨識，含權限請求與不支援時的文字後備）與 flutter_tts（老師唸英文）。麥克風錄音時鈕變紅並脈動。辨識完成把文字帶入輸入框並顯示發音吻合度回饋。
```

**指令 6 — 後端代理 + 真 AI 對話**
```
建立一個 Node.js/Express（或 FastAPI）後端，端點 /chat 接收對話歷史，於伺服器端帶入 LLM 金鑰呼叫模型並回傳。system prompt 依 Part B3 設定。App 端偵測到後端可用時切換為真 AI 自由對話，保留上下文。金鑰只在後端。
```

**指令 7 — 發音評分（本地、免費）**
```
在 App 端本地實作發音評分：用 speech_to_text 的辨識文字與信心值，跟目標句做詞級與音節級比對，算吻合度百分比並給分級評語。「聽正確發音」用 flutter_tts 即時合成（可調慢速），不放任何 MP3、不呼叫任何付費雲端服務。App 顯示分數環。
```

**指令 8 — 測驗與生字本**
```
做單字測驗頁（選擇題+解析+例句朗讀），答錯自動加入生字本；生字本支援 SRS 間隔複習排程。題庫用本地 sqflite/isar，依學測分級。
```

**指令 9 — 進度、打卡、遊戲化**
```
做「我的進度」頁：每日打卡連續天數、XP、等級、今日單字/對話/發音平均分圓環圖、徽章牆。加 flutter_local_notifications 每日提醒。
```

**指令 10 — iPhone 打包**
```
指導我在 Mac 上用 Xcode 打開 ios/ 專案、設定 Bundle ID 與簽章、以開發者帳號跑到我的 iPhone，並說明用 TestFlight 內測、最後上架 App Store 的流程與注意事項。
```

---

## Part D · 內容與升級路線（提醒 code 保留架構）

單字庫 10000 字採資料驅動、分批匯入（先學測核心 7000 字）。所有情境、題庫、老師形象都以資料檔擴充，不動核心程式。日後可加：AI 口說即時評測、學測模擬考、跨裝置雲端同步、班級／好友排行榜。

---

## Part E · 你需要自己準備的東西

要做到「真 AI」「沉浸式畫面」與「上 iPhone」，這些得由你提供：

- **LLM API 金鑰**（OpenAI 相容或 Claude，放後端）。
- **Mac + Xcode**（iOS 打包必需）與 **Apple Developer 帳號**（年費 US$99，上架/TestFlight 需要）。
- **角色素材**：兩位老師（帥哥/美女）的立繪，並綁定動畫（Rive 或 Live2D）——待機、說話嘴型、鼓勵、思考四種狀態。可先用 AI 生圖產出立繪再切分綁定，**商用務必確認授權**。
- **情境實景背景圖**：每個情境 1 張高解析場景圖（咖啡廳、機場、商店、書桌…），風格統一、含景深模糊，可 AI 生成或用授權圖庫。
- **語音（TTS/STT）**：不需要任何金鑰或費用——用手機內建離線引擎（flutter_tts / speech_to_text）。發音音素級評分屬選配且違反免費原則，預設不做。

---

*本規劃書搭配同資料夾的可執行原型 `ai_english_teacher.html`（網頁版，已具備老師選擇、情境對話、發音辨識、窮詞提醒、測驗、動態背景、可加入 iPhone 主畫面），可作為給 code 看的「互動參考範例」。*
