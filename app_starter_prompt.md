# AI 英語老師 App — 起手式 Prompt（直接複製給 code）

使用方式：把下面 ``` 區塊裡的整段文字，複製貼給你的 AI 編碼工具（Claude Code / Cursor 等）。
它會先建立專案骨架，做完會停下來等你，接著你再照「後續指令」一段一段餵。

---

```
你是一位資深 Flutter 工程師，要協助我從零打造一個 iOS/Android App。請先完整讀完我的規格，但「這一輪只做步驟一」，做完停下來等我確認，不要一次全部做完。

════════ 產品定位 ════════
一款給台灣高中生的「AI 英語口說老師」App。
- 使用者：台灣高中生，英文約 CEFR B1、單字量約 10000 字（學測 7000 字為核心）。
- 核心體驗：選老師形象 → 選情境 → 跟 AI 老師用英文面對面對話 → 即時糾正發音與用詞 → 卡住時提醒可用句子 → 穿插測驗與例句。
- 風格：要「生動、讓人每天想打開」，對標 TOKO AI / ELSA AI。

════════ 不可違反的硬性限制 ════════
1. 語音全部使用「手機作業系統內建的離線引擎」：老師發音用 flutter_tts（iOS AVSpeechSynthesizer / Android TextToSpeech，即時文字合成），學生發音辨識用 speech_to_text（iOS Speech / Android SpeechRecognizer）。
2. 嚴禁使用任何需付費或需 API Key 的雲端語音服務（Google Cloud TTS、OpenAI TTS、Amazon Polly、Azure Speech 等一律不可）。
3. 嚴禁在專案包內塞入單字 MP3 音檔——發音一律即時合成，10000 單字不存任何音檔，保持 App 輕量。
4. 發音評分只在本地做：用 speech_to_text 的辨識文字與信心值，跟目標句做詞級/音節級比對。
5. LLM 對話金鑰（若之後接真 AI）只能放在後端，App 內不得硬寫任何第三方金鑰。

════════ 技術棧 ════════
- Flutter 最新穩定版；狀態管理 Riverpod；路由 go_router。
- 本地資料：isar 或 sqflite（存單字庫、進度、打卡）。
- 語音：flutter_tts、speech_to_text（皆離線內建，見上方限制）。
- 後端（之後才做）：輕量 Node.js/Express 或 Python FastAPI，代理 LLM 對話並保管金鑰。
- 目標：可安裝到 iPhone（先開發者模式/TestFlight，之後上 App Store）。

════════ 視覺方向（之後對話頁要做到）════════
對話頁採沉浸式四層結構：
(1) 情境實景背景圖（點餐=咖啡廳、旅遊=機場、購物=商店…，含景深模糊、緩慢視差）；
(2) 老師角色立繪（Rive/Live2D，會眨眼、說話嘴型、點頭；帥哥 Ryan / 美女 Emma 可選）；
(3) 半透明毛玻璃對話框（英文 + 中文小字 + 🔊播放鈕）；
(4) 底部控制列（大麥克風圓鈕 + 語音波形 + 慢速/重播/翻譯）。
選單/載入頁則用會緩慢流動的多彩漸層動態背景（活潑、輕量）。

════════ 功能藍圖（分階段做，先別全做）════════
① 專案骨架 + 動態漸層背景 + 底部導覽  ← 這輪只做這個
② 選老師頁（帥哥/美女，選後設定對應 TTS 男/女聲）
③ 情境選單（生活/旅遊/購物/書信/點餐，資料驅動 JSON，可擴充）
④ 沉浸式對話頁 + 本地腳本引導對話
⑤ 語音輸入輸出（flutter_tts + speech_to_text，含權限與不支援時的文字後備）
⑥ 本地發音評分 + 聽正確發音（可調慢速）
⑦ 窮詞提醒（久沒回應/回答過短時，主動給句子與關鍵字）
⑧ 單字測驗 + 例句 + 生字本（SRS 間隔複習）
⑨ 每日進度、打卡連續天數、XP/等級/徽章、每日推播提醒
⑩ 接後端真 AI 自由對話（金鑰放後端）
⑪ iPhone 打包（Xcode 簽章 → 我的 iPhone → TestFlight → App Store）

════════ 這一輪的任務（步驟一）════════
只做這些，然後停下來：
1. 建立 Flutter 專案，採 features/ 分層資料夾：avatar、scenario、chat、pronunciation、quiz、progress、shared。
2. 設定深色主題、圓角、Noto Sans TC 字型。
3. 做一個全 App 可共用的 AnimatedBackground 元件：緩慢流動的多彩漸層（紫/粉/綠/橘），效能要好。
4. 用 go_router 做底部導覽三頁（對話 / 測驗 / 我的進度）與空白頁面骨架，先用假資料，不接任何語音或 API。

請輸出：
- 完整資料夾結構
- pubspec.yaml（列出上述依賴）
- AnimatedBackground 元件程式碼
- 主題設定與 main.dart（可直接執行）
- 在模擬器執行的步驟

做完請停下並問我「是否進入步驟二（選老師頁）」。
```

---

## 後續要餵的指令

步驟一完成後，依序把這些貼給 code（每段做完測過再下一段）：

- **步驟二**：做選老師頁（帥哥 Ryan / 美女 Emma 卡片，選取高亮，設定對應 flutter_tts 男/女聲）。
- **步驟三**：建 scenarios.json（五情境，含名稱/圖示/簡介/開場白/目標句型/關鍵字彙/背景圖），做資料驅動情境選單。
- **步驟四**：做沉浸式對話頁（四層結構），先用 JSON 腳本引導對話與本地比對評分。
- **步驟五**：整合 flutter_tts + speech_to_text（含權限與文字後備）。
- **步驟六～⑪**：發音評分、窮詞提醒、測驗生字本、進度打卡、後端真 AI、iPhone 打包。

（完整版每一步的細節，見同資料夾 `ai_teacher_app_prompt_plan.md` 的 Part C。）

---

## 小提醒
- 這份起手式已把你的兩個硬性限制（零金鑰語音、零 MP3）寫進去，code 會照做。
- 真正裝上 iPhone 需要 Mac + Xcode + Apple Developer 帳號（US$99/年）。
- 老師立繪與情境背景圖要另外準備（可先用 AI 生圖，商用注意授權）；沒圖之前 code 可先用色塊/佔位圖把版型做出來。
